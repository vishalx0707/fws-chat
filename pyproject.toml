[project]
name = "fws-chat"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "python-dotenv",
    "langchain-groq",
    "langchain-core",
    "groq",
    "python-multipart",
]

[tool.vercel]
entrypoint = "backend.main:app"
